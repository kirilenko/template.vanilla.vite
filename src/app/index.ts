import '../styles/main.css'

const app = () => {
  console.log('%c• template.vanilla.vite', 'color: magenta') // eslint-disable-line no-console
}

document.addEventListener('DOMContentLoaded', app)
